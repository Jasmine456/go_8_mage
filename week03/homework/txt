1.实现一个结构体同时“继承”另外两个结构体
2.定义结构体方法时，用结构体的值和指针有什么区别？用代码验证一下有没有对结构体进行拷贝
3.有一个长度为20的int数组，分别用两个for循环求前半部分的和和后半部分的积，要求只遍历一次数组
4.随机初始化两个8*5的矩阵，求两个矩阵的和（逐元素相加）？？
5.给定月份，判断属于哪个季节。分别用if和switch实现
6.创建一个student结构体，包含姓名和语数外三门课的成绩。用一个slice容纳一个班的同学，求每位同学的平均分和整个班三门课的平均分，全班同学平均分低于60的有几位
7.定义一个5行4列的float数组，把里面的元素全部放到一个一维切片里
8.arr:=make([]int,3,4);brr:=append(arr,1)问arr里的元素是什么？arr和brr会相互影响吗？写代码验证一下